<apex:page controller="IncentiveController">
     
    <apex:form >
    
     <apex:pageBlock id="master"> 
        <apex:pageBlockButtons >
            <apex:commandButton action="{!URLFOR($Page.InvNewIncentive)}" value="Create New Incentive"/>
        </apex:pageBlockButtons>
   
    
        <apex:pageBlockTable value="{!lstIncentives}" var="inc">
             <apex:column headerValue="Incentive Name" value="{!inc.name}"/>
             <apex:column headerValue="Is Active?" value="{!inc.Active__c}"/>
             <apex:column headerValue="Display Title" value="{!inc.Title__c}"/>
             <apex:column headerValue="Start Date" value="{!inc.StartDate__c}"/>
             <apex:column headerValue="End Date" value="{!inc.EndDate__c}"/>
             <apex:column headerValue="Url">
                <a href='{!inc.Url__c}' target='_new'>{!inc.Url__c}</a>
             </apex:column>
             <apex:column headerValue="Actions">
                 <apex:commandButton action="{!URLFOR($Page.InvEditIncentive)}" value="Edit"/>
                  <apex:commandButton action="{!removeIncentive}" value="Delete" rerender="master, hiddenBlock">
                      <apex:param name="incId" value="{!inc.Id}"/>
                   </apex:commandButton>
				   <apex:commandButton action="{!cloneIncentive}" value="Clone"/>
                   <apex:pageBlock id="hiddenBlock" rendered="false"></apex:pageBlock>
             </apex:column>
        </apex:pageBlockTable>
        
      </apex:pageBlock> 
    </apex:form>
     
</apex:page>